# Gamma API key, generate at https://gamma.app/settings/api-keys
# Requires Pro / Ultra / Teams / Business plan
GAMMA_API_KEY=sk-gamma-your-key-here

# Listen port (Railway sets this automatically in production)
PORT=3000
